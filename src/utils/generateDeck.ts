import { Deck } from "../deck/deck";
import { Card, CardValue } from "../cards";

const deck: Deck[] = [];


const suits: Suit[] = ;

const JOKERS: Card[] = [{
  suit: 'Joker',
  value: 100
}]

array.forEach(element => {
  
});